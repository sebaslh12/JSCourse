// Constant variable, it is used for semantinc purposes so they code its easier to understand
const isRaining = true

const person = {
    age: 27
}

person.age = 28 // We can change a property but not the variable itself
//person = {} // not valid

console.log(person);

console.log(isRaining);
