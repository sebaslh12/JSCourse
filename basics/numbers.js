let x = 11
let num = (x + 1) * 2 //operator precedence

console.log(num);

let age = 24
let dogYears = (age + 1) / 7

console.log(dogYears)

//Lecture Challenge: Calculate grade percent having the student score and the max score

let studentScore = 41
let maxScore = 100
let percent = (studentScore / maxScore) * 100

console.log(percent)